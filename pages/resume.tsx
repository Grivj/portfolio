import AppScreen from '../components/resume/AppScreen'
import { Container } from "@chakra-ui/react"

const Resume = () => (
    <Container p={["0", "0 10ch"]} maxWidth={"130ch"}>
        <AppScreen />
    </Container>
)

export default Resume